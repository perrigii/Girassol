<%# simple_form_for @review, url: new_user_appointment_review_path(user_id: @user.id, appointment_id: @appointment.id) do |f| %>
  <%# f.input :rating, placeholder: "Leave a review..." %>
  <%# f.text_area :comment, placeholder: "Leave a rating..." %>
  <%# f.submit "Submit", class: "btn btn-primary" %>
<%# end %>

<%= simple_form_for [@therapist, @review] do |f| %>
  <%= f.input :rating,
              collection: (1..5).to_a,
              placeholder: "Avalie...",
              input_html: {data: {controller: "star-rating"}} %>

  <%= f.text_area :comment, placeholder: "Deixe um comentário..." %>
  <%= f.submit "Enviar", class: "btn btn-primary" %>
<% end %>
